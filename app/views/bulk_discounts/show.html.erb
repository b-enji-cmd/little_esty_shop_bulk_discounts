<h1>Discount <%= @discount.id %></h1>
<section id ="discount-<%= @discount.id %>">
	<p><%= @discount.percentage_discount %></p>
	<p><%= @discount.quantity_threshold %></p>
	<%= link_to "Edit", edit_merchant_bulk_discount_path(@merchant.id, @discount.id), method: :get %>
</section>